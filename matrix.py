t = [[0 for _ in range(len(matrix))] for _ in range(len(matrix[0]] // matrix of transpose size

                                                        // Note row length becomes column length
                                                       // and vice versa
t = [[matrix[i][j] for i in range(len(matrix))] for j in range(len(matrix[0]))] // transpose of
                                                        matrix
